# Drop your images here

Filenames the app already expects (just save your real images with these
exact names and they'll appear automatically, no code changes needed):

- hero-gold-class.jpg        -> hero banner background (Images 2 in your Figma)
- business-banking-bg.jpg    -> "Business Banking" header band background (Image 4)
- news-anniversary.jpg       -> thumbnail used for the news/milestone cards (Image 6/7)

If you have a different image per news article later, edit the
`thumbnail` field per article in `src/api/mockApi.js` -> getNews().
